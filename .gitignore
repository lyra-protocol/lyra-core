node_modules
dist
.DS_Store

# Secrets
.env
.env.*
!.env.example
*.key.json

# The harvested dataset. Large, and rebuilt only by running.
.lyra/
*.db
*.db-wal
*.db-shm

# Runtime logs. The harvester appends on every cycle, so a tracked copy
# is permanent churn and tells you nothing the server does not already have.
harvest.log
*.log
